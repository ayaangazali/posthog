{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "anyOf": [
        {
            "properties": {
                "orgId": {
                    "description": "The organization to switch to: the `id` returned by `organizations-get` (a UUID-like string, not the organization name). Use `organizations-get` to resolve a name to its id.",
                    "type": "string"
                }
            },
            "required": ["orgId"],
            "type": "object"
        },
        {
            "properties": {
                "id": {
                    "description": "Alias for `orgId`. Accepts the `id` returned by `organizations-get`.",
                    "type": "string"
                }
            },
            "required": ["id"],
            "type": "object"
        }
    ]
}
